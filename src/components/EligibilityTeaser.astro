---
import { getEntry } from 'astro:content';
const entry = await getEntry('page', 'eligibility');
const { Content } = await entry!.render();
---
<aside class="eligibility-teaser" role="note">
  <h2>{entry!.data.title}</h2>
  <Content />
</aside>
