---
import { getEntry } from 'astro:content';
const entry = await getEntry('page', 'hero');
---
<section class="hero">
  <div class="hero-left">
    <span class="hero-eyebrow">Payment Support</span>
    <h1>{entry!.data.title}</h1>
  </div>
</section>
